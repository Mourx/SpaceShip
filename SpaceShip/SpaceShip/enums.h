#pragma once

enum MENU_STATE {
	WAITING,
	START
};

enum COMBAT_STEP {
	PLAYER_SETUP,
	PLAYER_TURN,
	AI_TURN,
};

enum GAME_SCREEN {
	MAIN_MENU,
	COMBAT_SCREEN,
	REWARD_SCREEN,
	LOSS_SCREEN,
	STAGE_SELECT
};

enum STAGE_TYPE {
	COMBAT,
	SHOP,
	TREASURE,
	BOSS
};

enum GAME_RESULT {
	ACTIVE,
	WIN,
	LOSS
};

enum REWARD_STATE {
	PICKING,
	PICKED
};

enum ChargeRarity {
	COMMON,
	UNCOMMON,
	RARE
};

enum COMMON_CHARGES {
	BASIC_CHARGE,
	BASIC_SHIELD
};

enum UNCOMMON_CHARGES {
	ATOM_BOMB,
	WIDE_SHIELD
};