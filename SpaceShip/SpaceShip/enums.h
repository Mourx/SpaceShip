#pragma once

enum COMBAT_STEP {
	PLAYER_SETUP,
	PLAYER_TURN,
	AI_TURN,
};

enum GAME_SCREEN {
	MAIN_MENU,
	COMBAT,
	REWARD_SCREEN,
	LOSS_SCREEN
};

enum GAME_RESULT {
	ACTIVE,
	WIN,
	LOSS
};

enum COMMON_CHARGES {
	BASIC_CHARGE,
	BASIC_SHIELD
};

enum UNCOMMON_CHARGES {
	ATOM_BOMB,
	WIDE_SHIELD
};